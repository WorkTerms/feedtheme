{%- assign size = site.feeds | size -%}
{%- if size == 0 -%}
	{{ content }}
{%- else -%}
	{%- include feeds/base/base.liquid -%}
	{%- assign sections = site.feeds 
		| sort: 'weight' 
		| where_exp:"item", "item.category == 'section'" -%}
{%- endif -%}
