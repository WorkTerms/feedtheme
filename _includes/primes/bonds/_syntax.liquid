{%- assign id = id | plus: 1            -%}
{%- assign parser = 329                 -%}
{%- assign syntax = id | minus: 329     -%}

{%- assign lexer = id
          | minus: syntax
          | minus: 60
          | minus: 60
          | minus: 40
          | minus: 1		            -%}
		  
{%- assign bond = lexer
          | minus: 60
          | minus: 6		            -%}
		  
{%- assign lexer = lexer
          | minus: bond		            -%}

{%- assign _syntax = _parser            -%}
{%- include core/_parts.liquid          -%}
