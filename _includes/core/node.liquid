{%- assign title = titles[layer]             -%}
{%- assign baseurl = title | prepend: "/"    -%}

{%-  assign title_order = '('
       | append: width
       | append: ') '
       | append: order                       -%}

{%-  assign stage = '['
       | append: num
       | append: ';'
       | append: order
       | append: ';'
       | append: width
       | append: ']('
       | append: weight
       | append: ';'
       | append: expanse
       | append: ';'
       | append: property
       | append: ')'                         -%}

{%-  assign details = '('
       | append: layer
       | append: ','
       | append: array_size
       | append: ')'
       | append: tag                         -%}

{%-  assign list = '<li data-id="'
       | append: stage
       | append: '"'                         -%}

{%-  assign id = width | plus: 168 | minus: 40           -%}
{%-  assign node = list
       | append: '><a href="'
       | append: baseurl
       | append: '" id='
       | append: id
       | append: ' aria-details="'
       | append: details
       | append: '">'
       | append: title_order
       | append: '</a></li>'                 -%}

{%-  capture nodes                           -%}
{{     nodes                                  }}
{%     include tabs.liquid size=14 %}{{ node  }}
{%-  endcapture                              -%}

