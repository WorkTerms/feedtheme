{%- assign tag = "[]"                     -%}

{%- assign primes = primes | push: num    -%}
{%- assign order = primes | size          -%}
{%- assign var = '' | split: ','          -%}
{%- assign probe = order                  -%}

{%-   include primes/bond.liquid          -%}

