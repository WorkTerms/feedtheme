{%- assign primes = '' | split: ','   -%}
{%- assign limit = 100 | times: 100   -%}

{%- unless nodes                      -%}
{%-   include data/primes.liquid      -%}
{%- endunless                         -%}

{%- assign nodes = nodes     
      | replace: "(0,)[]", "(0,0)[]"  -%}

{%- include core/_arm.liquid          -%}
