{%- comment -%}
*
*  index the feeds output
*
{%- endcomment -%}{%- capture my_capture %}
{% if page.path != 'README.md' -%}{%- include items.liquid -%}{%- endif -%}{%- for i in (1..my_size) %}{{ my_tab }}{% endfor %}}{% endcapture -%}{%- assign my_captures = my_captures | push: my_capture -%}{%- endfor -%}{%- assign my_feeds = '' | split: ',' -%}{%- for feed in feeds -%}{%- assign my_dir = feed.path | split: '/' | pop | join: '/' -%}{%- if my_dir == my_base -%}{%- assign my_feeds = my_feeds | push: feed -%}{%- endif -%}{%- endfor -%}
