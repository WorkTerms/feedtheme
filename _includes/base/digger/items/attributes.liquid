{%- comment -%}
*
*  collect attributes
*
{%- endcomment -%}{% assign myId = feed.myId -%}
{%- include tabs.liquid size=my_size %}{
{%- include tabs.liquid size=my_size %}{{ my_tab }}"id": {{ myId | jsonify }},{% if myId > 114 -%}{%- assign my_key = myId | minus: 101 -%}{%- else -%}{%- assign my_key = myId | minus: 1 -%}{%- endif %}
{%- include tabs.liquid size=my_size %}{{ my_tab }}"key": {{ layout.module[my_key] | jsonify }},